if [[ -z $1 ]]; then
    echo Error: Specify mark number for access 
    exit 1
fi

if [[ $1 == 9 || $1 == 10 ]]; then
    ssh -t k2woong92@mark8.postech.ac.kr ssh -i /home/k2woong92/.ssh/mark k2woong92@mark${1}.postech.ac.kr
else
    ssh k2woong92@mark${1}.postech.ac.kr
fi
