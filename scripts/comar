pushd ~/repos/custom-script > /dev/null

if ! [[ -z $(git status | grep clean) ]]; then
    echo there is no change
else
    git add -u
    git mcommit "update: auto"
    git push
fi

popd > /dev/null
